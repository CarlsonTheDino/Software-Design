#include "stdafx.h"
#include <iostream>
using std::cout;

int main()
{
	// While loop with at least one iteration
	do
	{
		// Loop body

		// Alter loop counter
		x = x + 1;
	} while (x < 5);

	// While loop with at least zero iterations

	while (y < 5)
	{
		// Loop body

		// Alter loop counter

		y = y + 1;
	}
}